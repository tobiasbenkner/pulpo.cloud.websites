---
import Container from "@/components/ui/Container.astro";

interface Props {
  quote: string;
  author: string;
}

const { quote, author } = Astro.props;
---

<section class="py-10 md:py-14 bg-surface/30 border-y border-border/30">
  <Container>
    <div class="max-w-3xl mx-auto text-center">
      <blockquote class="text-lg md:text-xl text-text italic leading-relaxed text-balance">
        {quote}
      </blockquote>
      <p class="mt-4 text-secondary text-sm font-medium">
        — {author}
      </p>
    </div>
  </Container>
</section>
